# Model checkpoints

The four PyTorch checkpoint files are artifacts from the research training process. The application currently uses `checkpoints/train_4.pt`.

Before claiming reproducibility, add a table for each checkpoint covering the model architecture, dataset version, class definitions, split, image size, training settings, selection metric, and checksum. Do not add private or non-consented training data to the repository.
